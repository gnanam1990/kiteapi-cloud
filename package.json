{
  "name": "kiteapi-cloud",
  "version": "0.1.0",
  "private": true,
  "description": "Paid API hosting and metering platform for Kite agent payments.",
  "type": "module",
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "dev": "pnpm --parallel --filter @kiteapi-cloud/api --filter @kiteapi-cloud/web dev",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@types/node": "^24.10.2",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
